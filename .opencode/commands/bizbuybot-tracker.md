---
description: BizBuyBot — summarize the current acquisition pipeline (deals, statuses, health)
---

# bizbuybot tracker

$ARGUMENTS

Load the bizbuybot skill and run the `tracker` mode:

```javascript
skill({ name: "bizbuybot" })
```

Follow `modes/tracker.md` exactly: run `node verify-pipeline.mjs` from the project root, read `data/acquisitions.md`, and summarize pipeline metrics.