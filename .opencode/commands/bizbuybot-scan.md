---
description: BizBuyBot — scan BizBuySell/BizQuest for new deal listings matching your criteria
---

# bizbuybot scan

$ARGUMENTS

Load the bizbuybot skill and run the `scan` mode:

```javascript
skill({ name: "bizbuybot" })
```

Follow `modes/scan.md` exactly: run `node scan.mjs` from the project root, review new entries in `data/pipeline.md`, and triage which to evaluate.